{
    "api_url": {
        "type": "string",
        "description": "XHS-Downloader 解析服务的 API 地址",
        "default": "http://127.0.0.1:5556/xhs/"
    },
    "enable_download_cache": {
        "type": "bool",
        "description": "是否启用下载缓存。开启后先下载到本地再发送，防止大文件超时。",
        "default": true
    },
    "enable_hybrid_mode": {
        "type": "bool",
        "description": "图文发送模式。开启(True)：智能合并相册(小于10MB) + 大图发文件。关闭(False)：强制所有图片以原图文件发送(不合并相册)。",
        "default": true
    }
}